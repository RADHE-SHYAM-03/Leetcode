class Solution {
public:
    bool detectCapitalUse(string word) {
        for(int i=0;i<word.size();i++){
            if(word[i]!=word[i]-'a'+'A'){
                return false;
            }
           
        }
        return true;
    }
};
